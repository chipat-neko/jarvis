name: Python CI

on:
  push:
    branches: [main]
    paths:
      - "services/**/*.py"
      - "services/**/pyproject.toml"
      - "pyproject.toml"
      - "proto/**"
      - "scripts/**.py"
      - "scripts/codegen_python.sh"
      - ".github/workflows/python-ci.yml"
  pull_request:
    branches: [main]
  workflow_dispatch:

# Annule les runs précédents sur la même branche
concurrency:
  group: python-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    name: Lint + Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install protoc + well-known types
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends protobuf-compiler libprotobuf-dev
          protoc --version

      - name: Install Python tooling (lint + test + codegen)
        run: |
          python -m pip install --upgrade pip
          pip install ruff>=0.8 mypy>=1.13 pytest>=8.0 pytest-asyncio>=0.24 grpcio grpcio-tools

      - name: Regenerate proto_gen (vérifie la codegen Python)
        run: bash scripts/codegen_python.sh

      - name: Install services (editable mode)
        run: |
          pip install -e services/jarvis-orchestrator
          pip install -e services/jarvis-llm
          pip install -e services/jarvis-safety
          # Les autres services seront installés au fil des sprints quand leurs deps seront vraiment utilisées.

      - name: ruff check
        run: ruff check

      - name: ruff format --check
        run: ruff format --check

      - name: pytest
        run: pytest -v
